# Text files check out with LF everywhere. The drift tests compare generated
# documents byte for byte, and a Windows checkout converting them to CRLF
# reports an identical file as stale.
* text=auto eol=lf

# Raw PTY captures, where a CRLF is terminal input rather than a line ending.
*.recording -text
